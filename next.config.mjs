const nextConfig = {
    images: {
        remotePatterns: [

            {
                protocol: 'https',
                hostname: 'bagoria.by',
                port: '',
                pathname: '/**',
            },
        ],
    },
};

export default nextConfig;
